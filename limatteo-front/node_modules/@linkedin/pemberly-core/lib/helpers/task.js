const MockUI = require('console-ui/mock');
const Command = require('./command-runner');

module.exports = class Task {
  constructor({ ui } = {}) {
    this.ui = ui || new MockUI();
  }

  run() {}

  createCommand(options) {
    return new Command(options);
  }
};
